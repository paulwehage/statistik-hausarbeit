on: [push, pull_request]

name: deploy-shiny

jobs:
  deploy-shiny:
    runs-on: macOS-latest
    steps:
      - uses: actions/checkout@v2
      - uses: r-lib/actions/setup-r@master
      - name: Install shiny
        run: |
          install.packages(c("shiny", "rsconnect"), type = "binary")
        shell: Rscript {0}
      - name: Push to shiny.io
        run: |
          Rscript -e "rsconnect::setAccountInfo(name='hypothesentest',token='791905CC71FCE699C8D95561426FDA1A', secret='zlZZsfDpzCU3ISBKa7qo0E9DgoAPciZ0KL4Z+kG8')"
          Rscript -e "rsconnect::deployApp(appName = 'hypothesentest')" 
